<p class="nested-fields">
  <span><%= f.label :name %></span>
  <span><%= f.text_field :name %></span>
  <span>Chronic Condition: No <%= f.radio_button :chronic, false %> Yes <%= f.radio_button :chronic, true %> </span>
  <span><%= link_to_remove_association "remove", f %></span>
</p>

