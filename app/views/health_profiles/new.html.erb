<%#= form_for @health_profile, :url => url_for(:controller => "health_profiles_controller", :action => "create") do |f|%>
<h1> Create Health Profile </h1>
<%= form_for [@patient, @health_profile] do |form|%>
<%= render partial: "health_profiles/form", object: form %>
<%= form.submit "Submit" %>
<% end %>
